# API base URL (usado pelo frontend em dev)
VITE_API_BASE_URL=http://localhost:3002

# URL pública do player (links no admin). Se vazio, usa VITE_API_BASE_URL.
# Em produção: ex. https://ava.seudominio.com.br
# VITE_PUBLIC_BASE_URL=
